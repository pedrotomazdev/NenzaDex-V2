{
  "scripts": {
    "prestart": "echo 'Rodando comando antes de iniciar servidor'",
    "start": "nodemon --ext js,html,css,json,svg js/server/server.js"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
